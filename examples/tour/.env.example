# Copy to examples/tour/.env (read by examples/demo_common/host.py after the environment
# and before the repo-root .env). Never put a real key in this file.
#
# The agent talks to whatever serves the Anthropic Messages API at ANTHROPIC_BASE_URL;
# the Anthropic SDK reads that variable and the key itself. TOUR_MODEL names the turn
# model and TOUR_MEMORY_MODEL the memory-extraction model in that provider's own ids;
# TOUR_MEMORY_MODEL falls back to TOUR_MODEL. Keep exactly one block uncommented.

# --- Anthropic (repo defaults: claude-sonnet-5 / claude-haiku-4-5) ---
ANTHROPIC_API_KEY=

# --- DeepSeek, Anthropic-compatible endpoint (https://api-docs.deepseek.com/guides/anthropic_api) ---
# ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# ANTHROPIC_API_KEY=
# TOUR_MODEL=deepseek-v4-flash
# TOUR_MEMORY_MODEL=deepseek-v4-flash

# --- Kimi (Moonshot), Anthropic-compatible endpoint (https://platform.kimi.com/docs) ---
# ANTHROPIC_BASE_URL=https://api.moonshot.cn/anthropic
# ANTHROPIC_API_KEY=
# TOUR_MODEL=kimi-k2.6
# TOUR_MEMORY_MODEL=kimi-k2.6

# --- What the agency calls itself (optional on either ERP) ---
# The store's own name and what the advisor calls the assistant. Unset, ACME's names stand.
# TOUR_BRAND_NAME=        # the agency's own name (default ACME 旅行社)
# TOUR_ASSISTANT_NAME=    # what the advisor calls the assistant (default 选团助手)

# --- The B2B 旅行社 ERP behind the advisor (optional) ---
# Unset, the example runs on the fixtures in data/. Set, api/main.py builds the HttpErpClient
# and logs in with the advisor's own ERP account; the reads then span every department that
# account is authorised for, and a write switches into the 团期's own. None of these ever
# reaches the model.
#
# Setting TOUR_ERP_BASE_URL also puts the example in live mode, where nothing in data/ stands
# for the agency: the advisor's name and department come from the ERP login, no memory is
# seeded, and 政策 questions are not answered at all, because the agency's rules are in its own
# knowledge base and nothing here reads it. The three variables below the credentials are what
# a live deployment has to name itself; without the customer or the 门店 the reads all work and
# a 占位 order is refused with the name of the variable that is missing.
# TOUR_ERP_BASE_URL=       # the ERP's /aicli root, HTTPS in production
# TOUR_ERP_MOBILE=         # the advisor's ERP login, a mainland mobile number
# TOUR_ERP_PASSWORD=       # its password; ten failed logins lock the account for 15 minutes
# TOUR_ERP_CUSTOMER_CODE=  # the 同行 customer's 客户编码 (csCode, e.g. HS00994); resolved to one
#                          # customer at boot, and quotes fall back to the 市场价 while it is not
# TOUR_ERP_STORE_NAME=     # the 门店 a 同行 order is written through, which the ERP requires
# TOUR_ERP_ALLOW_PAST=     # 1 lists departures that already left, for a beta with no future ones
